import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#82A185",
    },
    secondary: {
      main: "#B8D192",
    },
  },
});

export default theme;
